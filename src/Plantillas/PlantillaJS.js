/* =========================================
   Archivo: [Nombre del archivo con su extensión]
   Tipo: [JSX | JS | React Component]
   Descripción: [Función técnica del archivo o componente dentro del proyecto]
   ========================================= */

/* ====== IMPORTS ======
   Importaciones de librerías, hooks, componentes y recursos */

/* Ejemplo (no reproducible fuera de la plantilla):
import React from 'react';
import { Link } from 'react-router-dom';
import './Component.css'; */

/* ====== CONSTANTES / DATOS ======
   Datos estáticos, arrays de opciones, configuraciones internas */

/* Ejemplo (no reproducible fuera de la plantilla):
const skills = ['React', 'JS', 'CSS']; */

/* ====== HOOKS ======
   Estado y efectos del componente usando useState, useEffect, useRef, etc. */

/* Ejemplo (no reproducible fuera de la plantilla):
const [count, setCount] = useState(0);
useEffect(() => { ... }, []); */

/* ====== FUNCIONES / MÉTODOS ======
   Funciones auxiliares, handlers y callbacks */

/* Ejemplo (no reproducible fuera de la plantilla):
function handleClick() { ... } */

/* ====== RENDER / JSX ======
   Estructura principal del componente, return con JSX */

/* Ejemplo (no reproducible fuera de la plantilla):
return (
  <div className="component">
    <h1>{title}</h1>
  </div>
); */

/* ==================================================
   PLANTILLA DE COMENTARIOS INTERNOS (no reproducible fuera de la plantilla)

   Este bloque define cómo deben redactarse los
   comentarios dentro del archivo JS/React.
   No forma parte del código funcional.

   Reglas:
   - Describir función o propósito, no apariencia visual.
   - Usar lenguaje técnico, neutral y conciso.
   - Explicar comportamientos complejos o efectos colaterales.
   - Evitar comentarios redundantes o triviales.
   - Mantener coherencia con la estructura general del proyecto.
   - Para componentes: comentar props relevantes, hooks, efectos y callbacks.
   - Para funciones: comentar inputs, outputs y efectos secundarios.
   - Para bloques de JSX: explicar la estructura o intención del layout, no el estilo.
    ================================================== */